import { StyleSheet } from 'react-native';

export const novoCSS = StyleSheet.create({
    //Declaração do nome da classe
    qualquerCoisa: {
        backgroundColor: '#000444',
        flex: 1,
        justifyContent: 'center'
    },
    texto: {
        fontSize: 24,
        color: '#FFF'
    }
})